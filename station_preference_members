-- top 3 most popular stations used by member riders
SELECT 
    start_station_name,
    COUNT(*) as rides_count
FROM `portfolio-488812.bikeshare.cleaned_all`
WHERE member_casual = 'member'
GROUP BY start_station_name
ORDER BY rides_count DESC
LIMIT 3;
